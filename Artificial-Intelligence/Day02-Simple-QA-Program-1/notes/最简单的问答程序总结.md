# 基本流程
1. 创建调用API对象（服务认证、服务商服务器）
2. 创建获取请求回答函数（模型→信息→调用API接口，将列表信息按照对话形式发送给模型，让模型基于此信息生成回答→如果出现问题，就报错）
3. 创建主函数（输入问题→如果输入exit、quit、q就退出→如果输入为空就跳出此循环，进入到下一个循环→将问题作为参数调用获取请求回答函数→生成回答）
# 区别
相比Day1中的跑通第一个大模型，这里多了

1. temperature参数，用于指定模型生成内容的创意度
2. 由已经制定好的用户问题变为输入用户问题